import * as React from 'react';


interface Part2SubProps {
}

class Part2Sub extends React.Component<Part2SubProps, any> {
  public render() {
    return(
      <div>
        As before, we have already typed in the commands to start the two solvers. Go ahead and run the simulation with
        the updated configuration file.
      </div>
    );
  }
}

export default Part2Sub;


